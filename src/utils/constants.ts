// import styled from "styled-components";
import { Theme } from "../types/types";


export const THEME: Theme = {
    light: {
        background: 'red', 
        foreground: 'black',
        mode: 'light'
    }, 
    dark: {
        background: 'black', 
        foreground: 'red',
        mode: 'dark'
    }
}